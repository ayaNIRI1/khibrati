# Supabase Database Configuration
# Replace these with your actual Supabase project credentials to connect the database
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-service-role-key-or-anon-key

# Optional: Custom PORT for backend server (defaults to 5000)
PORT=5000
